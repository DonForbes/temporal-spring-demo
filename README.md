Demo application that has been split into two.


To run the UI
```
$ mvn spring-boot:run -pl temporal-demo-ui -f pom.xml
```

and to run the server (workers)
```
$  mvn spring-boot:run -pl temporal-demo-server -f pom.xml
```
